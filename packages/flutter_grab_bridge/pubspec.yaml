name: flutter_grab_bridge
description: Optional local CLI bridge for flutter_grab capture files.
version: 0.0.1
homepage: https://github.com/jaymutzafi/flutter_grab
resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.8
  flutter_lints: ^6.0.0

executables:
  flutter_grab_bridge:
